part of 'course_detail.dart';

class Competention extends ConsumerWidget {
  const Competention({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container();
  }
}
